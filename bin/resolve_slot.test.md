# Test SKILL.md fixture for resolve_slot.py

This is a fake SKILL.md to verify A6 resolver runtime PoC.

ContractSchema slot resolves to: `${MIC_v1.ContractSchema}`

LensSet slot resolves to: `${MIC_v1.LensSet}`

MethodologyConfig slot: `${MIC_v1.MethodologyConfig}`

Unknown slot (should warn): `${MIC_v1.NonExistentSlot}`

End of fixture.
