interface:
  display_name: "PI Workbench"
  short_description: "Coordinate PI work through evidence gates"
  default_prompt: "Use $pi-workbench to run this PI task through lease, measurement, and judgment gates."
policy:
  allow_implicit_invocation: false
