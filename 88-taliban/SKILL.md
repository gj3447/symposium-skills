---
name: 88-taliban
kg_ref: ATOM_Skill_88taliban
version: "3.1.0"
channel: stable
canonical_name: 88-나생문
aliases: [88-taliban, 88-naesengmoon, 113lens-naesengmoon]
provenance: AI_DERIVED_FROM_USER_PRIMARY  # SKILL.md = AI engineering; underlying methodology = user-primary mythology (12 apostles + 5 weapons). Per PseudepigraphaValidationGate-v1-2026-04-30.
description: >-
  Alias for Naesengmoon adversarial validation with the mathematical LensSet; `/88-taliban <target>` maps to `/taliban <target> --lens mathematical` and does not imply 88 independent critics. Invoke when: a target specifically needs the mathematical LensSet or the `/88-taliban` alias is requested. Do not use when: ordinary adversarial review needs another LensSet or user-selected cardinality; use `$taliban` instead.
---

# /88-taliban — `/taliban --lens mathematical` 별칭

> **이 스킬은 thin alias다.** 모든 로직은 `/taliban` 프레임워크에 있다.
> 본문 복제 = drift 유발. 하지 마라.

## 사용법

```
/88-taliban <target> [--depth quick|standard|deep]
```

**동일 명령:**
```
/taliban <target> --lens mathematical [--depth quick|standard|deep]
```

## 실행

1. `/taliban` SKILL.md 로드
2. `--lens mathematical` 자동 설정
3. 나머지 전부 Naesengmoon 프레임워크 프로토콜 따름

## 명명 주의 — "88" ≠ 113 (2026-05-27 ceremony 정정)

> KG: `ceremony-verdict-88-35-vs-113-2026-05-27` (identity_resolved=FALSE_DISTINCT)

이 skill의 "88"은 **역사적 명칭**(88-army 통칭)이고, `--lens mathematical`이 실제 resolve하는 것은 **113-lens 명명정리 set**(`LensSet:mathematical` = Polytope Euler / Adjunction triangle / Nash equilibrium 류)이다.

per-node 개념비교(2026-05-27)로 확인: 이름의 "88"이 가리키는 **88 adjectival 구조속성 술어 family**(`LensSet:mathematical-predicate-88` = is-X-acyclic/bounded/commutative/monolithic...)와 이 skill이 invoke하는 **113 명명정리 set**은 **별개 family**(개념 overlap 15%, count 88≠113). 따라서:

- `/88-taliban` = `--lens mathematical` = **113 명명정리** lens (이 skill의 default).
- 88 adjectival 술어를 쓰려면 `--lens mathematical-predicate-88` 명시 (별도 LensSet).
- 과거 "88-taliban = 113 lens" 등식은 두 family 혼동 = drift였음 (본 정정으로 해소).

## MIC Binding

**IS slot**: `MetaVerifier` (MIC_v1.currentConcrete = "88-Naesengmoon")
→ 실체는 Naesengmoon 프레임워크 + LensSet:mathematical (113 명명정리). `mathematical-predicate-88`은 별도 LensSet.

# KG: ATOM_Skill_88taliban, alias-of-taliban, lensset-mathematical
