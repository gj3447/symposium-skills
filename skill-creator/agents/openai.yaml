interface:
  display_name: "Legacy Skill Creator"
  short_description: "Invoke the legacy SYMPOSIUM skill creator explicitly"
  default_prompt: "Use $skill-creator only for this explicit legacy skill workflow."
policy:
  allow_implicit_invocation: false
