interface:
  display_name: "APT Meta Review"
  short_description: "Run the internal APT meta-review phase explicitly"
  default_prompt: "Use $apt-meta-review to classify this bounded APT cycle evidence and return a local or qualified PENDING handoff."
policy:
  allow_implicit_invocation: false
