interface:
  display_name: "APT Fix Agent"
  short_description: "Invoke the internal APT fix specialist explicitly"
  default_prompt: "Use $fix-agent for this explicit blocker correction task."
policy:
  allow_implicit_invocation: false
